[build]
dockerfilePath = "web/Dockerfile"

[deploy]
startCommand = "uvicorn web.backend.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100

[env]
DATABASE_URL = { fromRailwayVariable = "DATABASE_URL" }
